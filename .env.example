# Required settings - CHANGE THESE BEFORE DEPLOYING
SECRET_KEY=generate-a-random-secret-key-here
ADMIN_PASSWORD=your-secure-admin-password

# HackPSU Authentication
AUTH_LOGIN_URL=https://your-hackpsu-app.com/login
AUTH_ENVIRONMENT=production
MIN_JUDGE_ROLE=2
MIN_ADMIN_ROLE=4

# HackPSU API
HACKPSU_API_BASE_URL=https://apiv3.hackpsu.org

# Firebase Configuration (for API sync)
# To enable sync, you need a Firebase service account:
# 1. Go to Firebase Console > Project Settings > Service Accounts
# 2. Click "Generate new private key" and download the JSON file
# 3. Save it as firebase-service-account.json in this directory
# 4. Set GOOGLE_APPLICATION_CREDENTIALS below
# 5. Uncomment the volumes section in docker-compose.yml
FIREBASE_PROJECT_ID=hackpsu-408118
FIREBASE_API_KEY=AIzaSyBG636oXijUAzCq6Makd2DNU_0WzPJRw8s
GOOGLE_APPLICATION_CREDENTIALS=/app/firebase-service-account.json

# Alternative: Manually provide a service token (not recommended - expires hourly)
# HACKPSU_SERVICE_TOKEN=

# Sync settings
SYNC_INTERVAL_MINUTES=30

# Database (set automatically in docker-compose)
# DATABASE_URL=postgresql://user:password@host:5432/dbname

# Redis (set automatically in docker-compose)
# REDIS_URL=redis://host:6379/0

# Email (optional - disabled by default in Docker)
DISABLE_EMAIL=true
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_FROM=your-email@example.com
# EMAIL_USER=your-email@example.com
# EMAIL_PASSWORD=your-app-password
